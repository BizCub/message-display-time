{
    "schemaVersion": 1,
    "id": "${id}",
    "name": "${name}",
    "version": "${version}",
    "description": "${description}",
    "license": "${license}",
    "icon": "icon.png",
    "authors": [
        "${author}"
    ],
    "contact": {
        "homepage": "https://modrinth.com/project/${modrinth}",
        "issues": "https://github.com/BizCub/${github}/issues"
    },
    "entrypoints": {
        "main": [
            "com.bizcub.messageDisplayTime.platform.Fabric"
        ]
    },
    "accessWidener" : "${mixin}.ct",
    "mixins": [
        "${mixin}.mixins.json"
    ]
}
